class _dataControl
{
    element echart;

    //takes in general data object (label mapped to data) from dataloader
    //and converts into an object usable by new Chart to initialise
    //a chart
    object chartConvert(object data);
}